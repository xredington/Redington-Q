import AdminDashboard from '@/views/admin/Dashboard';
import React from 'react';

const Page = () => {
    return (
        <main  className={`w-95 mx-auto`}>
            <AdminDashboard/>
        </main>
);
};

export default Page;